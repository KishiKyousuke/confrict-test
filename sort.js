var sortNumber = function (nunber) {
	number.sort(function (a,b) {
		ig (a === b) {
			return 0;
		}
		return a < b ? -1 : 1;
	});
};

var number = [19, 3, 81, 1, 2 , 21];
sortNumber(number);)
console.log(number);
